---

- name: Install Openshift

  hosts: local

  vars_files:
    - /vars/aws-credentials
    - /vars/aws-config.yml

  roles:
  - aws-setup-vpc

  post_tasks:
   - name: test
     debug:
      msg: ' {{ region }}'

